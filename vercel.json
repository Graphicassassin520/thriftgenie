{
  "version": 2,
  "buildCommand": "prisma generate && next build",
  "devCommand": "next dev",
  "framework": "nextjs",
  "functions": {
    "app/api/*/route.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "OPENAI_API_KEY": "@openai-api-key",
    "DATABASE_URL": "@database-url"
  },
  "build": {
    "env": {
      "OPENAI_API_KEY": "@openai-api-key",
      "DATABASE_URL": "@database-url"
    }
  },
  "crons": []
} 